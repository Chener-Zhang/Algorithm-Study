package com.company.TreeAlgorithm;

import java.util.Deque;
import java.util.LinkedList;

public class widthOfBinaryTree {
    public int widthOfBinaryTree(TreeNode root) {
//        Deque<Pair<TreeNode, Integer>> dequeue = new LinkedList<>();
//        dequeue.add(root);
//        int max = 0;
//        while (!dequeue.isEmpty()) {
//            int len = dequeue.size();
//            if (len > 1) {
//                max = Math.max(dequeue.peekLast().val - dequeue.peekFirst().val, max);
//            }
//            for (int i = 0; i < len; i++) {
//                TreeNode current = dequeue.removeFirst();
//                if (current.left != null) dequeue.add(current.left);
//                if (current.right != null) dequeue.add(current.right);
//            }
//        }
//        System.out.println(max);
//        return max;
        return 0;
    }
}
